const superHeroes: string[] = [];
// const superPower: number[] = [];
const superPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 255, 255]];

superHeroes.push("spiderman");
superPower.push(3);
allUsers.push({ name: "Rifki", isActive: true });
